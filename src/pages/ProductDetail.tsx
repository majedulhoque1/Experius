/*
  Product pages use the same template as client case studies — same schema,
  same sections, same metric honesty rules. A product should have to prove
  itself the way a client project does.
*/
export { default } from './CaseStudy'
